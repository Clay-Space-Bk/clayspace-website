import TeamMain from '@/components/clayspace/TeamMain';
import { Metadata } from 'next';

export const metadata: Metadata = { title: "Clay Space — Meet the Team" };

export default function page() { return <TeamMain />; }
