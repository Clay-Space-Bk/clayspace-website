import StyleGuideMain from '@/components/clayspace/StyleGuideMain';
import { Metadata } from 'next';

export const metadata: Metadata = { title: "Clay Space — Style Guide" };

export default function page() { return <StyleGuideMain />; }
