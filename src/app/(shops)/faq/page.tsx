import FaqMain from '@/components/clayspace/FaqMain';
import { Metadata } from 'next';

export const metadata: Metadata = { title: "Clay Space — FAQ" };

export default function page() { return <FaqMain />; }
